<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.edu.swpu.cins.dao.OrgMapDao">
    <select id="getMap" resultType="OrgMap">
        SELECT org_id, org_name FROM org_map;
    </select>
</mapper>